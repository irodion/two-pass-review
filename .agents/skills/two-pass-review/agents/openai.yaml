interface:
  display_name: "Two-Pass Review"
  short_description: "Two-pass code review, rendered as an HTML report"
  default_prompt: "Run a Two-Pass Review of this branch, PR, or working tree: both
    rubric passes, merged findings, and the HTML report. Print the report path."
policy:
  allow_implicit_invocation: false
